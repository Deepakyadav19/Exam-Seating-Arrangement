const employeeModel = require("./employee");
const classroomModel = require("./classroom");
const arrangementModel = require("./arrrangement");

module.exports = {
  employee: employeeModel,
  arrangement: arrangementModel,
  classroom: classroomModel,
};
